let analysisForm = document.getElementById("analysisForm");
analysisForm.onsubmit = function(event) {
    event.preventDefault();
// Alert replaced by Real Api
    alert("Proposal submitted for analysis!");
};